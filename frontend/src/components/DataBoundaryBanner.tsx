export default function DataBoundaryBanner() {
  return (
    <div className="banner">
      Data boundary: every record shown is synthetic. This prototype is not validated software and
      must not be used for real change control, quality, or regulatory decisions.
    </div>
  );
}
